# 项目经验

## 本地浏览器验证

- 调整 `next.config.mjs` 会重启开发服务器。重启期间不要立即执行响应式视口切换和元素坐标读取；应先读取一次页面 DOM 确认路由恢复，再进行交互验证，避免把服务重载时序误判为组件问题。

## 品牌资产

- 不要把低分辨率位图自动描摹成近似 SVG 后直接作为品牌资产交付；轮廓偏差会比位图模糊更损害识别度。没有官方矢量源文件时，默认保留原品牌图，并只处理尺寸、裁切和光学对齐。
